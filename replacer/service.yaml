apiVersion: v1
kind: Service
metadata:
  name: {{ include "replacer.fullname" . }}
  labels:
    {{- include "replacer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "replacer.selectorLabels" . | nindent 4 }}
